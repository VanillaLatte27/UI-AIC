# Cloudflare Tunnel Configuration
# Get your tunnel token from: https://dash.cloudflare.com/profile/api-tokens
# Create a tunnel at: https://dash.cloudflare.com/teams/your-team/network/tunnels
TUNNEL_TOKEN=your-cloudflare-tunnel-token-here

# Optional: Custom domain (if you have one configured)
# TUNNEL_HOSTNAME=your-domain.com

# AI Backend Configuration
FLASK_ENV=production
DEFAULT_VIDEO=pasar
DEFAULT_TARGET=Fajar

# Frontend Configuration
NEXT_PUBLIC_AI_BACKEND_URL=http://localhost:5001
