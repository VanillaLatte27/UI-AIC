# Frontend (.env) example for production
# Public env is embedded into client bundle at build time
NEXT_PUBLIC_FLASK_BASE_URL=https://smart.backend.muhammadhaggy.com

# Server-side API route base URL (used by Next.js API routes at runtime)
FLASK_API_URL=https://smart.backend.muhammadhaggy.com

# Optional
# NODE_ENV=production
# PORT=3000
